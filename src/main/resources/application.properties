spring.application.name=backend-pulseira

# Usa o Atlas quando MONGODB_URI estiver definida; usa MongoDB local como alternativa.
spring.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/pulseira_db}
spring.data.mongodb.auto-index-creation=true
spring.mongodb.database=pulseira_db
